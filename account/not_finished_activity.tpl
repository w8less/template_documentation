<{extends 'layout/layout.tpl'}><{block name="template-name"}>    /account/info.tpl<{/block}><{block name=title}>    <{$module.name|htmlspecialchars}><{/block}><{block name="module"}>    <h1>        Что произошло    </h1>    <div class="row">        <div class="col-lg-3">            <nav class="nav flex-column">                <{foreach $module.menu as $menuItem}>                    <a class="nav-link<{if $menuItem.is_active}> active<{/if}>" href="<{$menuItem.url}>">                        <{t $menuItem.short_name}>                    </a>                <{/foreach}>            </nav>        </div>        <div class="col-lg-9">            123            - лайки комментриев            - ответы на комментарии            - собственные лайки            - собственные статьи            (Каждая активность в виде строки:            - Дата/время            - Тип элемента (кликабельный с переходом элемент на сайте)            - Имя юзера (со ссылкой на страницу юзера (когда будет страница юзера публичная)            )        </div>    </div><{/block}>